language: node_js
node_js:
  - "0.10"
git:
  submodules: false
install:
  - npm install -g grunt
  - npm install -g casperjs
  - npm install
  - bower install
before_script:
  - phantomjs --version
  - casperjs --version
script: "casperjs test test/phantomcss/tests.js"
